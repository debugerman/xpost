# API token for protecting xpost endpoints.
# If empty in local non-container mode, xpost can auto-generate on first boot.
# In Docker/Compose, set explicitly for predictable access.
XPOST_API_TOKEN=replace-with-strong-token

# Choose ONE auth mode:
# 1) OAuth1 (recommended for full media compatibility)
X_API_KEY=
X_API_SECRET=
X_ACCESS_TOKEN=
X_ACCESS_TOKEN_SECRET=

# 2) OAuth2 user token (alternative)
# X_OAUTH2_ACCESS_TOKEN=
